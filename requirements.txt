# Web server
flask>=3.0.0,<4.0.0
gunicorn>=22.0.0,<24.0.0
flask-cors>=4.0.0,<6.0.0

# Environment
python-dotenv>=1.0.0,<2.0.0

# Model download
huggingface-hub>=0.23.0,<1.0.0

# Image processing
pillow>=10.0.0,<12.0.0

# PyTorch CPU wheels (smaller Docker images; GPU hosts can override at build time)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0,<3.0.0
torchvision>=0.17.0,<1.0.0
